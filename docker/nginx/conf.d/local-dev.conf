# Configuration for local development with web running on host
# This configuration redirects web traffic to local development server

# Override the default upstream to point to local dev server
upstream web_override {
    server host.docker.internal:3000;
}

# Note: The actual server block is in default.conf.template
# We're just defining the upstream here to redirect traffic